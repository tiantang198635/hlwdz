#配置mybatis
mybatis:
  mapperLocations: classpath:mapper/*.xml

server:
  port: 8085
      
spring:
  profiles:
    active: me
  application:
    name: sign-user

jwt:
  header: Authorization
  secret: mySecret
  expiration: 604800
  tokenHead: "Bearer "
  
#cookie保存token时长
cookie:
  maxage: 3600

header:
  username: CUR_USERNAME